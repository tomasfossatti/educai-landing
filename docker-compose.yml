services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: educai
    ports:
      - "5432:5432"
    volumes:
      - educai_pg:/var/lib/postgresql/data
volumes:
  educai_pg:
